#:kivy 1.0
#:import kivy kivy
#:import win kivy.core.window
#:import split os.path.split

<empty@BGTemplate>:
    size: 50,50
    text: ""
    src:""
    Image:
        id: image
        source: root.src
        allow_stretch: True
        #keep_ratio: False
        size_hint: 1,1

